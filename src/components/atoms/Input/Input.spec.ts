import { describe, it } from "vitest";

describe('Input', () => {
    it('should move the label up', {}, () => {})
    it('should show an error message when the value is unvalid', {}, () => {})
    it('should update the value to the selected mask', {}, () => {})
});