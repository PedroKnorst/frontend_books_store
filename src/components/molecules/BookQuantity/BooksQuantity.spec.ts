import { describe, it } from 'vitest';

describe('BooksQuantity', () => {
  it('should increase quantity when up button is clicked ', { todo: true }, () => {});
  it('should decrease quantity when down button is clicked ', { todo: true }, () => {});
});
